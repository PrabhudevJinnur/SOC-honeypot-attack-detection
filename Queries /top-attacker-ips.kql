// =====================================
// Top Attacker IP Addresses
// =====================================
// Identifies source IPs responsible for the
// highest number of failed logon attempts.

SecurityEvent
| where EventID == 4625
| summarize FailedAttempts = count() by IpAddress
| order by FailedAttempts desc

